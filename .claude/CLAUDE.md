# Claude Memory - Global Context
# このファイルはClaude Codeが起動時に読み込むグローバルコンテキストです

## Coding Standards

- Use descriptive variable and function names
- Add comments for complex logic
- Follow language-specific conventions
- Keep functions small and focused

## AI Coding Workflow

- Always check `git status` before making changes
- Use `git diff` to review changes before committing
- Write meaningful commit messages
- Test changes before committing

## Preferred Commands

For file operations:
- Use `cat` to read file contents
- Use `ls -la` for detailed directory listings
- Use `tree` for directory structure visualization

For Git operations:
- Use `git status` frequently
- Use `git diff` to review changes
- Use `git log --oneline --graph` for history

## Environment

- Primary shell: zsh
- Operating System: macOS/Linux
- Package manager: Homebrew (macOS)
